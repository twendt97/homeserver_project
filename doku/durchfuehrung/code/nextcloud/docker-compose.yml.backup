version: '3.1'

services:

  nextcloud:
    image: nextcloud
    restart: always
    ports:
      - 80
    volumes:
      - ../volumes/nextcloud:/var/www/html
    environment:
      MYSQL_HOST: db
      MYSQL_PASSWORD: OHM_TUM_USC_db_nc
      MYSQL_DATABASE: db_nextcloud
      MYSQL_USER: nextcloud
      VIRTUAL_HOST: cloud.localhost
    networks:
      - frontend
      - backend

networks:
  frontend:
    external:
      name: frontend
  backend:
    external:
      name: backend
